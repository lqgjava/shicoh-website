{
  "name": "shicoh-shouye",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "vercel-build": "vite build",
    "predploy": "npm run prepare-deploy",
    "deploy": "gh-pages -d deploy",
    "prepare-deploy": "mkdir deploy && xcopy /E /I /Y index.html deploy\\ && xcopy /E /I /Y css deploy\\css && xcopy /E /I /Y js deploy\\js && xcopy /E /I /Y assets deploy\\assets"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.4.0"
  }
}
